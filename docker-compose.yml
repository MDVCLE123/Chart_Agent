version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - AWS_REGION=${AWS_REGION:-us-east-1}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - HEALTHLAKE_DATASTORE_ENDPOINT=${HEALTHLAKE_DATASTORE_ENDPOINT}
      - BEDROCK_MODEL_ID=${BEDROCK_MODEL_ID:-us.anthropic.claude-sonnet-4-20250514-v1:0}
      - USE_DEMO_MODE=${USE_DEMO_MODE:-true}
      - DEMO_FHIR_SERVER=${DEMO_FHIR_SERVER:-https://hapi.fhir.org/baseR4}
      - EPIC_CLIENT_ID=${EPIC_CLIENT_ID:-5f2384c3-5bb4-484f-a528-068177894d81}
      - EPIC_PRIVATE_KEY_PATH=/app/keys/epic_private_key.pem
      - ATHENA_CLIENT_ID=0oa104juaipwtISjQ298
      - ATHENA_PRIVATE_KEY_PATH=/app/keys/epic_private_key.pem
      - ATHENA_PRACTICE_ID=${ATHENA_PRACTICE_ID:-195900}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY:-chart-agent-secret-key-change-in-production-2024}
      # Cognito Configuration (REQUIRED - set in .env file)
      - USE_COGNITO=${USE_COGNITO:-true}
      - COGNITO_REGION=${COGNITO_REGION}
      - COGNITO_USER_POOL_ID=${COGNITO_USER_POOL_ID}
      - COGNITO_CLIENT_ID=${COGNITO_CLIENT_ID}
      - COGNITO_CLIENT_SECRET=${COGNITO_CLIENT_SECRET}
      - ENVIRONMENT=development
      - CORS_ORIGINS=["http://localhost:3000","http://localhost:80"]
    volumes:
      - ./backend/app:/app/app  # Hot reload for development
      - ./keys:/app/keys  # Epic keys for JWT auth
    networks:
      - chart-agent-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    environment:
      - REACT_APP_API_URL=http://localhost:8000
    depends_on:
      - backend
    networks:
      - chart-agent-network

networks:
  chart-agent-network:
    driver: bridge

